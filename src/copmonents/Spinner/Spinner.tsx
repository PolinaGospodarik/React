import "./Spinner.css"

const Spinner = () => {
    return (
        <>
            <div className="loader-wrap">
                <span className="loader"></span>
            </div>

        </>
    );
};

export default Spinner;